// npm init -y  ==> package.json
// npm i -D typescript   ==> node_modules
// npx tsc --init     ==> Created a new tsconfig.json

// After that create these folders   ==>

    // File Layout
    // "rootDir": "./src",
    // "outDir": "./dist",

// write typescript in src/index.ts

// npx tsc  ==> created js and another thighs in dist      //  you can also create this script in package.json=> "dev": "npx tsc"

// run node dist/index.js   // you can also create this script in package.json=> "start": "node dist/index.js"